# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secrets
JWT_SECRET=super_secret_key_change_me_in_production_987654

# MySQL Database Details (Optional - App falls back to SQLite3 automatically if empty)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=begin_upsc
